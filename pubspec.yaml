name: opticalqrtransfer
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  path_provider: ^0.4.1
  barcode_scan: ^1.0.0
  qr_flutter: ^2.1.0+55
  flutter_screenutil: ^0.5.2
  file_picker: ^1.3.6
  simple_permissions: ^0.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
